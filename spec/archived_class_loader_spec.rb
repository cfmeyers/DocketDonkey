require 'archived_class_loader'

describe ArchivedClassLoader do
  #reads a csv file (all csv files in directory?)
  #converts them all into parsed_csv objects
  #checks for duplicate case_numbers in db
  #if it finds a duplicate, the one with the most filled out fields is kept

end
